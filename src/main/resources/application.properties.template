# Database Configuration
db.url=jdbc:postgresql://localhost:5432/YOUR_DB_NAME
db.user=YOUR_DB_USERNAME
db.password=YOUR_DB_PASSWORD

# Paths to other configs (point to separate files)
cloudinary.config.path=classpath:cloudinary.properties
google.credentials.path=classpath:credentials.json